{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "target/release/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "//#biome": {},
    "//#biome:fix": {
      "cache": false
    }
  }
}
